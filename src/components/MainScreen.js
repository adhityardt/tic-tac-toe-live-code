import React, { Component } from 'react';
import {Text, View} from 'react-native'
class MainScreen extends Component {
  constructor(props) {
    super(props);
    this.state = {  };
  }
  render() {
    return (
      <Text> ini halaman main screen</Text>
    );
  }
}

export default MainScreen;